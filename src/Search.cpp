#include "Search.h"
#include <iostream>
/*
Item& Search::BinarySearch(Item* array, int searchValue, int MAX_SIZE)
{
	int min{ 0 };
	int max{ MAX_SIZE };

	while (true)
	{
		if (min >= max && array[min].goldValue != searchValue)
		{
			Item bad;
			return bad; 
		}

		int middle =  min + (max - min) / 2;
		std::cout <<"Array middle: "<<array[middle].goldValue<< '\n';

		if (array[middle].goldValue > searchValue) // Too big? Search down
		{
			max = middle - 1;
		}
		else if (array[middle].goldValue < searchValue) // Too small? Search up
		{
			min = middle + 1;
		}
		else {
			return array[middle];
		}
	} 
}
*/


